apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-pod               # MUST match the label in deployment.yaml
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
  